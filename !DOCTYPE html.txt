<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>THE RAMADAN CUP 2026</title>
<link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700;900&family=Oswald:wght@400;600;700&family=Cairo:wght@400;600;700;900&family=Amiri:wght@700&display=swap" rel="stylesheet">
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: linear-gradient(135deg, #0a0a0a 0%, #1a0a00 50%, #0d0d0d 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    font-family: 'Cairo', sans-serif;
    overflow-x: hidden;
  }

  .poster {
    width: 560px;
    min-height: 950px;
    background: linear-gradient(160deg, #0d0d0d 0%, #1a0a00 30%, #2d1810 50%, #1a0a00 70%, #0d0d0d 100%);
    position: relative;
    overflow: hidden;
    border: 3px solid #c8860a;
    box-shadow: 
      0 0 80px rgba(200,134,10,0.5), 
      0 0 150px rgba(200,134,10,0.2),
      inset 0 0 100px rgba(200,134,10,0.1);
  }

  /* Islamic Pattern Overlay */
  .poster::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: 
      radial-gradient(circle at 20% 80%, rgba(200,134,10,0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(200,134,10,0.1) 0%, transparent 50%),
      repeating-linear-gradient(45deg, transparent, transparent 35px, rgba(200,134,10,0.04) 35px, rgba(200,134,10,0.04) 36px),
      repeating-linear-gradient(-45deg, transparent, transparent 35px, rgba(200,134,10,0.04) 35px, rgba(200,134,10,0.04) 36px);
    z-index: 0;
    animation: patternShift 20s linear infinite;
  }

  @keyframes patternShift {
    0% { transform: translate(0, 0); }
    100% { transform: translate(35px, 35px); }
  }

  /* Glowing Moon Background */
  .moon-bg {
    position: absolute;
    top: -100px;
    left: 50%;
    transform: translateX(-50%);
    width: 500px;
    height: 500px;
    background: 
      radial-gradient(circle at 30% 30%, rgba(255,215,0,0.3) 0%, rgba(200,134,10,0.2) 30%, transparent 70%),
      radial-gradient(circle, rgba(200,134,10,0.15) 0%, transparent 60%);
    border-radius: 50%;
    z-index: 0;
    animation: moonPulse 4s ease-in-out infinite;
  }

  @keyframes moonPulse {
    0%, 100% { transform: translateX(-50%) scale(1); opacity: 0.8; }
    50% { transform: translateX(-50%) scale(1.05); opacity: 1; }
  }

  /* Crescent Moon Shape */
  .crescent-moon {
    position: absolute;
    top: 40px;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 120px;
    border-radius: 50%;
    box-shadow: -20px 5px 0 5px #FFD700;
    z-index: 1;
    animation: crescentGlow 3s ease-in-out infinite alternate;
  }

  @keyframes crescentGlow {
    from { filter: drop-shadow(0 0 20px rgba(255,215,0,0.6)); }
    to { filter: drop-shadow(0 0 40px rgba(255,215,0,1)); }
  }

  /* Ornate Corners */
  .corner {
    position: absolute;
    width: 100px;
    height: 100px;
    z-index: 2;
  }

  .corner-tl { 
    top: 0; left: 0; 
    border-top: 4px solid #c8860a; 
    border-left: 4px solid #c8860a;
    background: linear-gradient(135deg, rgba(200,134,10,0.3) 0%, transparent 70%);
  }
  .corner-tr { 
    top: 0; right: 0; 
    border-top: 4px solid #c8860a; 
    border-right: 4px solid #c8860a;
    background: linear-gradient(-135deg, rgba(200,134,10,0.3) 0%, transparent 70%);
  }
  .corner-bl { 
    bottom: 0; left: 0; 
    border-bottom: 4px solid #c8860a; 
    border-left: 4px solid #c8860a;
    background: linear-gradient(45deg, rgba(200,134,10,0.3) 0%, transparent 70%);
  }
  .corner-br { 
    bottom: 0; right: 0; 
    border-bottom: 4px solid #c8860a; 
    border-right: 4px solid #c8860a;
    background: linear-gradient(-45deg, rgba(200,134,10,0.3) 0%, transparent 70%);
  }

  /* Islamic Geometric Pattern Borders */
  .islamic-border {
    position: absolute;
    width: 100%;
    height: 40px;
    background: 
      linear-gradient(90deg, transparent 0%, rgba(200,134,10,0.3) 20%, rgba(200,134,10,0.3) 80%, transparent 100%),
      repeating-linear-gradient(90deg, transparent, transparent 20px, rgba(255,215,0,0.5) 20px, rgba(255,215,0,0.5) 22px, transparent 22px, transparent 42px);
    z-index: 2;
  }
  .border-top { top: 100px; }
  .border-bottom { bottom: 100px; }

  /* Stars with Glow */
  .stars {
    position: absolute;
    inset: 0;
    z-index: 0;
  }
  .star {
    position: absolute;
    width: 4px;
    height: 4px;
    background: #FFD700;
    border-radius: 50%;
    box-shadow: 0 0 10px #FFD700, 0 0 20px #FFD700;
    animation: twinkle 2s infinite alternate;
  }
  @keyframes twinkle {
    from { opacity: 0.3; transform: scale(0.8); }
    to { opacity: 1; transform: scale(1.3); }
  }

  /* Floating Lanterns */
  .lanterns {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }

  .lantern {
    position: absolute;
    font-size: 50px;
    filter: drop-shadow(0 0 20px rgba(255,200,50,0.9)) drop-shadow(0 0 40px rgba(255,100,0,0.5));
    animation: swing 4s ease-in-out infinite;
    transform-origin: top center;
  }

  @keyframes swing {
    0%, 100% { transform: rotate(-8deg) translateY(0); }
    50% { transform: rotate(8deg) translateY(-10px); }
  }

  .lantern-1 { top: 80px; left: 20px; animation-delay: 0s; font-size: 55px; }
  .lantern-2 { top: 100px; right: 30px; animation-delay: 0.7s; font-size: 50px; }
  .lantern-3 { top: 180px; left: 10px; animation-delay: 1.4s; font-size: 40px; }
  .lantern-4 { top: 150px; right: 15px; animation-delay: 2.1s; font-size: 45px; }
  .lantern-5 { top: 250px; left: 5px; animation-delay: 2.8s; font-size: 35px; }
  .lantern-6 { top: 220px; right: 5px; animation-delay: 3.5s; font-size: 38px; }

  /* Floating Decorations */
  .ramadan-deco {
    position: absolute;
    font-size: 30px;
    filter: drop-shadow(0 0 15px rgba(200,134,10,0.8));
    animation: float-deco 5s ease-in-out infinite;
    z-index: 1;
  }

  @keyframes float-deco {
    0%, 100% { transform: translateY(0) rotate(0deg) scale(1); }
    50% { transform: translateY(-15px) rotate(10deg) scale(1.1); }
  }

  .deco-1 { top: 300px; left: 30px; animation-delay: 0.3s; }
  .deco-2 { top: 350px; right: 35px; animation-delay: 0.9s; }
  .deco-3 { top: 450px; left: 25px; animation-delay: 1.5s; }
  .deco-4 { top: 500px; right: 30px; animation-delay: 2.1s; }
  .deco-5 { bottom: 250px; left: 35px; animation-delay: 2.7s; }
  .deco-6 { bottom: 200px; right: 25px; animation-delay: 3.3s; }

  /* Light Rays Effect */
  .light-rays {
    position: absolute;
    top: -200px;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 600px;
    background: conic-gradient(from 0deg, transparent 0deg, rgba(255,215,0,0.1) 10deg, transparent 20deg, transparent 40deg, rgba(255,215,0,0.1) 50deg, transparent 60deg, transparent 80deg, rgba(255,215,0,0.1) 90deg, transparent 100deg, transparent 120deg, rgba(255,215,0,0.1) 130deg, transparent 140deg, transparent 160deg, rgba(255,215,0,0.1) 170deg, transparent 180deg, transparent 200deg, rgba(255,215,0,0.1) 210deg, transparent 220deg, transparent 240deg, rgba(255,215,0,0.1) 250deg, transparent 260deg, transparent 280deg, rgba(255,215,0,0.1) 290deg, transparent 300deg, transparent 320deg, rgba(255,215,0,0.1) 330deg, transparent 340deg, transparent 360deg);
    animation: rotateRays 30s linear infinite;
    z-index: 0;
    pointer-events: none;
  }

  @keyframes rotateRays {
    from { transform: translateX(-50%) rotate(0deg); }
    to { transform: translateX(-50%) rotate(360deg); }
  }

  .content {
    position: relative;
    z-index: 3;
    padding: 40px 35px 35px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
  }

  .top-icons {
    display: flex;
    justify-content: space-between;
    width: 100%;
    margin-bottom: 10px;
    padding: 0 20px;
  }
  .ball-icon {
    font-size: 48px;
    filter: drop-shadow(0 0 15px rgba(200,134,10,0.8));
    animation: float 3s ease-in-out infinite;
  }
  .ball-icon:last-child { animation-delay: 1.5s; }
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-10px) rotate(10deg); }
  }

  .title-section {
    text-align: center;
    margin-bottom: 15px;
    position: relative;
  }

  .title-the {
    font-family: 'Cinzel Decorative', serif;
    font-size: 20px;
    color: #c8860a;
    letter-spacing: 8px;
    text-transform: uppercase;
    opacity: 0.9;
    text-shadow: 0 0 10px rgba(200,134,10,0.5);
  }

  .title-main {
    font-family: 'Cinzel Decorative', serif;
    font-size: 52px;
    font-weight: 900;
    line-height: 1.1;
    background: linear-gradient(180deg, #FFD700 0%, #FFA500 30%, #c8860a 60%, #8B4513 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 4px 12px rgba(200,134,10,0.6));
    animation: titleGlow 3s ease-in-out infinite alternate;
  }

  @keyframes titleGlow {
    from { filter: drop-shadow(0 4px 12px rgba(200,134,10,0.6)); }
    to { filter: drop-shadow(0 4px 20px rgba(255,215,0,0.9)); }
  }

  .title-cup {
    font-family: 'Cinzel Decorative', serif;
    font-size: 44px;
    font-weight: 700;
    background: linear-gradient(180deg, #FFD700 0%, #FFA500 50%, #c8860a 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 3px 10px rgba(200,134,10,0.6));
  }

  .title-year {
    font-family: 'Oswald', sans-serif;
    font-size: 60px;
    font-weight: 700;
    color: #fff;
    letter-spacing: 6px;
    text-shadow: 0 0 30px rgba(255,215,0,0.5), 0 0 60px rgba(255,215,0,0.3);
    animation: yearPulse 2s ease-in-out infinite alternate;
  }

  @keyframes yearPulse {
    from { text-shadow: 0 0 30px rgba(255,215,0,0.5), 0 0 60px rgba(255,215,0,0.3); }
    to { text-shadow: 0 0 40px rgba(255,215,0,0.8), 0 0 80px rgba(255,215,0,0.5); }
  }

  /* Arabic Calligraphy Style Subtitle */
  .subtitle {
    font-family: 'Amiri', serif;
    font-size: 28px;
    color: #FFD700;
    letter-spacing: 4px;
    text-align: center;
    margin-bottom: 10px;
    text-shadow: 0 0 20px rgba(255,215,0,0.6);
    position: relative;
  }

  .subtitle::before, .subtitle::after {
    content: '❧';
    margin: 0 15px;
    color: #c8860a;
  }

  .divider {
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, transparent, #c8860a, #FFD700, #FFA500, #FFD700, #c8860a, transparent);
    margin: 15px 0;
    position: relative;
  }
  .divider::after {
    content: '☪';
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    background: #1a0a00;
    padding: 0 15px;
    font-size: 24px;
    color: #FFD700;
    text-shadow: 0 0 10px rgba(255,215,0,0.8);
  }

  /* Subscription Fee Section */
  .subscription-section {
    width: 100%;
    background: 
      linear-gradient(135deg, rgba(220,20,60,0.2) 0%, rgba(139,0,0,0.15) 100%),
      repeating-linear-gradient(45deg, transparent, transparent 5px, rgba(220,20,60,0.05) 5px, rgba(220,20,60,0.05) 10px);
    border: 3px solid rgba(220,20,60,0.6);
    border-radius: 15px;
    padding: 20px;
    margin: 10px 0;
    text-align: center;
    box-shadow: 0 0 25px rgba(220,20,60,0.2);
    position: relative;
    overflow: hidden;
  }

  .subscription-section::before {
    content: '💰';
    position: absolute;
    left: 20px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 35px;
    animation: bounce 2s infinite;
  }

  .subscription-section::after {
    content: '💰';
    position: absolute;
    right: 20px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 35px;
    animation: bounce 2s infinite 0.5s;
  }

  .subscription-label {
    font-family: 'Cairo', sans-serif;
    font-size: 16px;
    color: #DC143C;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 8px;
    text-shadow: 0 0 10px rgba(220,20,60,0.4);
  }

  .subscription-amount {
    font-family: 'Oswald', sans-serif;
    font-size: 42px;
    font-weight: 900;
    color: #FF6347;
    text-shadow: 0 0 20px rgba(220,20,60,0.6);
  }

  .prizes-container {
    width: 100%;
    margin: 10px 0;
  }

  .prizes-title {
    text-align: center;
    font-family: 'Oswald', sans-serif;
    font-size: 18px;
    color: #FFD700;
    letter-spacing: 5px;
    text-transform: uppercase;
    margin-bottom: 12px;
    text-shadow: 0 0 15px rgba(255,215,0,0.5);
  }

  .prize-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 12px 25px;
    margin-bottom: 10px;
    border-radius: 10px;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .prize-card:hover {
    transform: translateX(-5px) scale(1.02);
  }

  .prize-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transform: translateX(-100%);
    transition: transform 0.5s;
  }

  .prize-card:hover::before {
    transform: translateX(100%);
  }

  .prize-first {
    background: linear-gradient(135deg, rgba(255,215,0,0.2) 0%, rgba(200,134,10,0.15) 100%);
    border: 2px solid rgba(255,215,0,0.6);
    box-shadow: 0 0 30px rgba(255,215,0,0.2), inset 0 0 20px rgba(255,215,0,0.05);
  }

  .prize-second {
    background: linear-gradient(135deg, rgba(192,192,192,0.15) 0%, rgba(150,150,150,0.1) 100%);
    border: 2px solid rgba(192,192,192,0.4);
    box-shadow: 0 0 20px rgba(192,192,192,0.15);
  }

  .prize-third {
    background: linear-gradient(135deg, rgba(205,127,50,0.15) 0%, rgba(160,100,40,0.1) 100%);
    border: 2px solid rgba(205,127,50,0.4);
    box-shadow: 0 0 20px rgba(205,127,50,0.15);
  }

  .prize-fourth {
    background: linear-gradient(135deg, rgba(100,149,237,0.15) 0%, rgba(70,130,180,0.1) 100%);
    border: 2px solid rgba(100,149,237,0.4);
    box-shadow: 0 0 20px rgba(100,149,237,0.15);
  }

  .prize-rank {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .rank-medal {
    font-size: 32px;
    filter: drop-shadow(0 0 8px rgba(200,134,10,0.8));
    animation: medalBounce 2s infinite;
  }

  @keyframes medalBounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-3px); }
  }

  .rank-text {
    font-family: 'Cairo', sans-serif;
    font-size: 18px;
    font-weight: 700;
  }

  .prize-first .rank-text { color: #FFD700; text-shadow: 0 0 10px rgba(255,215,0,0.5); }
  .prize-second .rank-text { color: #E0E0E0; }
  .prize-third .rank-text { color: #CD7F32; }
  .prize-fourth .rank-text { color: #87CEEB; }

  .prize-amount {
    font-family: 'Oswald', sans-serif;
    font-weight: 700;
    font-size: 26px;
  }
  .prize-first .prize-amount { color: #FFD700; text-shadow: 0 0 15px rgba(255,215,0,0.6); }
  .prize-second .prize-amount { color: #E0E0E0; text-shadow: 0 0 10px rgba(192,192,192,0.4); }
  .prize-third .prize-amount { color: #CD7F32; text-shadow: 0 0 10px rgba(205,127,50,0.4); }
  .prize-fourth .prize-amount { color: #87CEEB; }

  /* Total Prizes - Enhanced */
  .total-prizes {
    width: 100%;
    background: 
      linear-gradient(135deg, rgba(255,215,0,0.25) 0%, rgba(200,134,10,0.2) 100%),
      repeating-linear-gradient(45deg, transparent, transparent 10px, rgba(255,215,0,0.05) 10px, rgba(255,215,0,0.05) 20px);
    border: 3px solid rgba(255,215,0,0.6);
    border-radius: 15px;
    padding: 20px;
    margin: 15px 0;
    text-align: center;
    box-shadow: 0 0 30px rgba(255,215,0,0.25), inset 0 0 30px rgba(255,215,0,0.05);
    position: relative;
    overflow: hidden;
  }

  .total-prizes::before {
    content: '✦';
    position: absolute;
    left: 20px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 30px;
    color: #FFD700;
    animation: spin 4s linear infinite;
  }

  .total-prizes::after {
    content: '✦';
    position: absolute;
    right: 20px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 30px;
    color: #FFD700;
    animation: spin 4s linear infinite reverse;
  }

  @keyframes spin {
    from { transform: translateY(-50%) rotate(0deg); }
    to { transform: translateY(-50%) rotate(360deg); }
  }

  .total-label {
    font-family: 'Cairo', sans-serif;
    font-size: 16px;
    color: #FFD700;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 8px;
    text-shadow: 0 0 10px rgba(255,215,0,0.5);
  }

  .total-amount {
    font-family: 'Oswald', sans-serif;
    font-size: 42px;
    font-weight: 900;
    color: #FFD700;
    text-shadow: 0 0 30px rgba(255,215,0,0.8), 0 0 60px rgba(255,215,0,0.4);
    animation: totalGlow 2s ease-in-out infinite alternate;
  }

  @keyframes totalGlow {
    from { text-shadow: 0 0 30px rgba(255,215,0,0.8), 0 0 60px rgba(255,215,0,0.4); }
    to { text-shadow: 0 0 40px rgba(255,215,0,1), 0 0 80px rgba(255,215,0,0.6); }
  }

  /* Registration Link - Enhanced */
  .registration-link {
    display: block;
    width: 100%;
    background: 
      linear-gradient(135deg, rgba(200,134,10,0.3), rgba(255,215,0,0.15)),
      repeating-linear-gradient(45deg, transparent, transparent 5px, rgba(255,215,0,0.05) 5px, rgba(255,215,0,0.05) 10px);
    border: 3px solid #c8860a;
    border-radius: 15px;
    padding: 18px 25px;
    margin: 10px 0;
    text-align: center;
    text-decoration: none;
    color: inherit;
    position: relative;
    transition: all 0.3s;
    box-shadow: 0 0 20px rgba(200,134,10,0.2);
  }

  .registration-link:hover {
    transform: scale(1.03) translateY(-2px);
    box-shadow: 0 10px 30px rgba(200,134,10,0.4);
    background: linear-gradient(135deg, rgba(200,134,10,0.4), rgba(255,215,0,0.25));
  }

  .reg-label {
    font-family: 'Cairo', sans-serif;
    font-size: 16px;
    color: #FFD700;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
  }

  .reg-text {
    font-family: 'Oswald', sans-serif;
    font-size: 32px;
    font-weight: 700;
    color: #FFD700;
    letter-spacing: 2px;
    text-shadow: 0 0 20px rgba(255,215,0,0.7);
  }

  /* WhatsApp Contacts Section */
  .whatsapp-contacts {
    width: 100%;
    background: 
      linear-gradient(135deg, rgba(37,211,102,0.15), rgba(18,140,126,0.1)),
      repeating-linear-gradient(-45deg, transparent, transparent 5px, rgba(37,211,102,0.05) 5px, rgba(37,211,102,0.05) 10px);
    border: 3px solid #25D366;
    border-radius: 15px;
    padding: 20px;
    margin: 10px 0;
    text-align: center;
    box-shadow: 0 0 20px rgba(37,211,102,0.2);
  }

  .whatsapp-title {
    font-family: 'Cairo', sans-serif;
    font-size: 16px;
    color: #25D366;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
  }

  .whatsapp-numbers {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .whatsapp-number-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background: rgba(37,211,102,0.1);
    border: 2px solid rgba(37,211,102,0.3);
    border-radius: 10px;
    padding: 12px;
    text-decoration: none;
    color: inherit;
    transition: all 0.3s;
  }

  .whatsapp-number-link:hover {
    background: rgba(37,211,102,0.2);
    transform: scale(1.02);
    box-shadow: 0 5px 15px rgba(37,211,102,0.3);
  }

  .whatsapp-icon {
    font-size: 24px;
  }

  .whatsapp-number-text {
    font-family: 'Oswald', sans-serif;
    font-size: 28px;
    font-weight: 700;
    color: #fff;
    letter-spacing: 2px;
    text-shadow: 0 0 15px rgba(37,211,102,0.8);
    direction: ltr;
  }

  /* Info Cards Container */
  .info-container {
    display: flex;
    gap: 10px;
    width: 100%;
    margin: 10px 0;
  }

  .date-section, .location-section {
    flex: 1;
    background: linear-gradient(135deg, rgba(200,134,10,0.2), rgba(255,100,0,0.1));
    border: 2px solid rgba(200,134,10,0.5);
    border-radius: 12px;
    padding: 15px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .location-section {
    background: linear-gradient(135deg, rgba(138,43,226,0.2), rgba(75,0,130,0.1));
    border-color: rgba(138,43,226,0.5);
  }

  .date-label, .location-label {
    font-family: 'Cairo', sans-serif;
    font-size: 12px;
    color: #aaa;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 5px;
  }

  .date-value, .location-value {
    font-family: 'Oswald', sans-serif;
    font-size: 22px;
    font-weight: 700;
    color: #FFD700;
    letter-spacing: 1px;
  }

  .location-value {
    color: #DA70D6;
    text-shadow: 0 0 10px rgba(218,112,214,0.5);
  }

  .date-icon, .location-icon { 
    font-size: 32px; 
    margin-bottom: 5px;
    display: block;
  }

  /* Ramadan Greeting - Enhanced */
  .ramadan-greeting {
    width: 100%;
    text-align: center;
    padding: 15px;
    margin-top: 15px;
    background: linear-gradient(90deg, transparent, rgba(200,134,10,0.2), transparent);
    border-top: 1px solid rgba(200,134,10,0.3);
    border-bottom: 1px solid rgba(200,134,10,0.3);
  }

  .ramadan-greeting p {
    font-family: 'Amiri', serif;
    font-size: 32px;
    color: #FFD700;
    font-weight: 700;
    text-shadow: 0 0 20px rgba(255,215,0,0.6), 0 0 40px rgba(255,215,0,0.3);
    animation: greetingPulse 3s ease-in-out infinite;
  }

  @keyframes greetingPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }

  .bottom-strip {
    width: 100%;
    background: linear-gradient(90deg, transparent, #c8860a, #FFD700, #FFA500, #FFD700, #c8860a, transparent);
    height: 4px;
    margin-top: 15px;
    border-radius: 2px;
    box-shadow: 0 0 10px rgba(200,134,10,0.5);
  }

  .bottom-deco {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 15px;
    font-size: 40px;
    filter: drop-shadow(0 0 15px rgba(200,134,10,0.8));
  }

  /* Icon animations */
  .bounce {
    animation: bounce 2s infinite;
    display: inline-block;
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }

  /* Floating Particles */
  .particles {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
    overflow: hidden;
  }

  .particle {
    position: absolute;
    width: 6px;
    height: 6px;
    background: #FFD700;
    border-radius: 50%;
    opacity: 0.6;
    animation: floatUp 8s linear infinite;
    box-shadow: 0 0 10px #FFD700;
  }

  @keyframes floatUp {
    0% { transform: translateY(100vh) scale(0); opacity: 0; }
    10% { opacity: 0.6; }
    90% { opacity: 0.6; }
    100% { transform: translateY(-100px) scale(1); opacity: 0; }
  }

</style>
<base target="_blank">
</head>
<body>
<div class="poster">
  <div class="light-rays"></div>
  <div class="moon-bg"></div>
  <div class="crescent-moon"></div>

  <!-- Floating Particles -->
  <div class="particles">
    <div class="particle" style="left:10%;animation-delay:0s;"></div>
    <div class="particle" style="left:20%;animation-delay:1s;"></div>
    <div class="particle" style="left:30%;animation-delay:2s;"></div>
    <div class="particle" style="left:40%;animation-delay:3s;"></div>
    <div class="particle" style="left:50%;animation-delay:4s;"></div>
    <div class="particle" style="left:60%;animation-delay:5s;"></div>
    <div class="particle" style="left:70%;animation-delay:6s;"></div>
    <div class="particle" style="left:80%;animation-delay:7s;"></div>
    <div class="particle" style="left:90%;animation-delay:2.5s;"></div>
  </div>

  <!-- Stars -->
  <div class="stars">
    <div class="star" style="top:8%;left:8%;animation-delay:0.3s"></div>
    <div class="star" style="top:12%;left:85%;animation-delay:0.7s"></div>
    <div class="star" style="top:5%;left:45%;animation-delay:1.1s"></div>
    <div class="star" style="top:25%;left:92%;animation-delay:0.5s"></div>
    <div class="star" style="top:55%;left:5%;animation-delay:1.3s"></div>
    <div class="star" style="top:70%;left:88%;animation-delay:0.9s"></div>
    <div class="star" style="top:80%;left:20%;animation-delay:0.2s"></div>
    <div class="star" style="top:85%;left:65%;animation-delay:1.6s"></div>
    <div class="star" style="top:40%;left:3%;animation-delay:0.8s;width:3px;height:3px"></div>
    <div class="star" style="top:18%;left:60%;animation-delay:1.4s;width:3px;height:3px"></div>
    <div class="star" style="top:35%;left:75%;animation-delay:1.8s"></div>
    <div class="star" style="top:65%;left:25%;animation-delay:2.2s"></div>
  </div>

  <!-- Lanterns -->
  <div class="lanterns">
    <div class="lantern lantern-1">🏮</div>
    <div class="lantern lantern-2">🏮</div>
    <div class="lantern lantern-3">🪔</div>
    <div class="lantern lantern-4">🪔</div>
    <div class="lantern lantern-5">🏮</div>
    <div class="lantern lantern-6">🪔</div>
  </div>

  <div class="ramadan-deco deco-1">✨</div>
  <div class="ramadan-deco deco-2">🌙</div>
  <div class="ramadan-deco deco-3">✨</div>
  <div class="ramadan-deco deco-4">🌙</div>
  <div class="ramadan-deco deco-5">⭐</div>
  <div class="ramadan-deco deco-6">✨</div>

  <div class="islamic-border border-top"></div>
  <div class="islamic-border border-bottom"></div>

  <div class="corner corner-tl"></div>
  <div class="corner corner-tr"></div>
  <div class="corner corner-bl"></div>
  <div class="corner corner-br"></div>

  <div class="content">

    <div class="top-icons">
      <div class="ball-icon">⚽</div>
      <div class="ball-icon">⚽</div>
    </div>

    <div class="title-section">
      <div class="title-the">THE</div>
      <div class="title-main">RAMADAN</div>
      <div class="title-cup">CUP</div>
      <div class="title-year">2026</div>
    </div>

    <div class="subtitle">بطولة رمضانية</div>

    <div class="divider"></div>

    <!-- Subscription Fee -->
    <div class="subscription-section">
      <div class="subscription-label">💵 رسوم الاشتراك</div>
      <div class="subscription-amount">2,500 ج.م</div>
    </div>

    <div class="divider"></div>

    <!-- Prizes -->
    <div class="prizes-container">
      <div class="prizes-title">🏆 جوائز البطولة</div>

      <div class="prize-card prize-first">
        <div class="prize-rank">
          <span class="rank-medal">🥇</span>
          <span class="rank-text">المركز الأول</span>
        </div>
        <div class="prize-amount">18,000 ج.م</div>
      </div>

      <div class="prize-card prize-second">
        <div class="prize-rank">
          <span class="rank-medal">🥈</span>
          <span class="rank-text">المركز الثاني</span>
        </div>
        <div class="prize-amount">8,000 ج.م</div>
      </div>

      <div class="prize-card prize-third">
        <div class="prize-rank">
          <span class="rank-medal">🥉</span>
          <span class="rank-text">المركز الثالث</span>
        </div>
        <div class="prize-amount">4,000 ج.م</div>
      </div>

      <div class="prize-card prize-fourth">
        <div class="prize-rank">
          <span class="rank-medal">🏅</span>
          <span class="rank-text">المركز الرابع</span>
        </div>
        <div class="prize-amount">جوائز قيمة 🎁</div>
      </div>
    </div>

    <!-- Total Prizes -->
    <div class="total-prizes">
      <div class="total-label">💰 إجمالي جوائز البطولة</div>
      <div class="total-amount">30,000 ج.م</div>
    </div>

    <div class="divider"></div>

    <!-- Registration Link -->
    <a href="https://forms.gle/KbtAQNAuevx6eVu4A" class="registration-link" target="_blank" rel="noopener noreferrer">
      <div class="reg-label">
        <span class="bounce">📋</span>
        <span>سجل الآن</span>
        <span class="bounce">📋</span>
      </div>
      <div class="reg-text">اضغط للتسجيل في البطولة</div>
    </a>

    <!-- WhatsApp Contacts -->
    <div class="whatsapp-contacts">
      <div class="whatsapp-title">
        <span class="bounce">📱</span>
        <span>تواصل واتساب</span>
        <span class="bounce">💬</span>
      </div>
      <div class="whatsapp-numbers">
        <a href="https://wa.me/201124306267?text=مرحباً%2C%20أريد%20الاستفسار%20عن%20بطولة%20رمضان%20كوب%202026" class="whatsapp-number-link" target="_blank" rel="noopener noreferrer">
          <span class="whatsapp-icon">💬</span>
          <span class="whatsapp-number-text">01124306267</span>
        </a>
        <a href="https://wa.me/201129657240?text=مرحباً%2C%20أريد%20الاستفسار%20عن%20بطولة%20رمضان%20كوب%202026" class="whatsapp-number-link" target="_blank" rel="noopener noreferrer">
          <span class="whatsapp-icon">💬</span>
          <span class="whatsapp-number-text">01129657240</span>
        </a>
      </div>
    </div>

    <!-- Date & Location -->
    <div class="info-container">
      <div class="date-section">
        <div class="date-icon">📅</div>
        <div class="date-label">موعد البطولة</div>
        <div class="date-value">28 فبراير 2026</div>
      </div>

      <div class="location-section">
        <div class="location-icon">📍</div>
        <div class="location-label">مكان البطولة</div>
        <div class="location-value">زهراء المعادي</div>
      </div>
    </div>

    <div class="ramadan-greeting">
      <p>🌙 رمضان كريم 🌙</p>
    </div>

    <div class="bottom-strip"></div>

    <div class="bottom-deco">
      🏆 ⚽ 🏆
    </div>

  </div>
</div>
</body>
</html>